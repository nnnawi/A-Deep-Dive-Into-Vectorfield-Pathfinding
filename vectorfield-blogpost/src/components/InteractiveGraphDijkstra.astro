---
// Interactive Graph with Dijkstra Visualization
const { title = "Interactive Graph Dijkstra" } = Astro.props;
---

<div class="p5-component">
  <div id="interactive_graph_djikstra_canva"></div>
</div>

<script>
  import interactive_graph_djikstra_sketch from '../../../scripts/interactive_graph_djikstra.js';

  // Initialize p5 instance with the sketch
  const initInteractiveGraphDijkstra = () => {
    if (typeof window !== 'undefined' && window.p5) {
      new p5((p) => interactive_graph_djikstra_sketch(p, 'interactive_graph_djikstra_canva'));
    }
  };

  // Wait for p5 to be loaded
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initInteractiveGraphDijkstra);
  } else {
    initInteractiveGraphDijkstra();
  }
</script>

<style>
  .p5-component {
    margin: 2rem 0;
    display: flex;
    justify-content: center;
  }

  #interactive_graph_djikstra_canva {
    border: 2px solid #dee2e6;
    border-radius: 4px;
    overflow: hidden;
    background: white;
  }
</style>
